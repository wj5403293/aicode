<!-- 项目规则：AGENTS.md 硬规则 -->
## 项目规则（硬规则）
- 项目根目录（`~/workspace`）或 `~/.aicode` 下可能存在 `AGENTS.md`，记录该项目的专属规则、架构约束与构建指南。
- 严格遵守 `AGENTS.md`，其优先级高于本提示词的一切通用规则。若根目录无 `AGENTS.md` 但有 `CLAUDE.md`，以 `CLAUDE.md` 为准，效力相同。
